{
 "validator": "ux",
 "instance": 1,
 "per_prototype": [
  {
   "prototype": "A",
   "findings": [
    {
     "id": "UX-A1",
     "severity": "High",
     "confidence": 88,
     "element": "Three commit models on one page: instant-save toggles (notifications, autocharge), the sticky `.savebar` triggered by `dirty()` on the details form, and the drawer's 'Review & confirm'",
     "finding": "One screen teaches three different rules for when a change takes effect, and the only thing distinguishing them is a sentence of body copy under the Notifications heading.",
     "user_impact": "Users who learn one model apply it to the others \u2014 either walking away from unsaved address edits or waiting for a save bar that will never appear for a toggle.",
     "suggested_fix": "Pick one commit model for the whole page, or give instantly-saved controls a persistent visual marker (inline '\u2713 Saved' on every one) so the two classes are distinguishable without reading prose."
    },
    {
     "id": "UX-A2",
     "severity": "High",
     "confidence": 85,
     "element": "720px right drawer (`#drawer`) with live total in the footer, over a scrimmed page whose 'What you're paying for' table shows the current $92.00 total",
     "finding": "The current cost breakdown and the proposed new total are never on screen together \u2014 the drawer and its scrim cover the page's breakdown table.",
     "user_impact": "Users must hold the old figure in memory to judge whether the new one is acceptable, which is exactly the comparison that makes a paid change feel safe.",
     "suggested_fix": "Show 'now \u2192 new' as two columns in the drawer footer, or render the delta ('+$18.00/mo') beside the new total rather than the total alone."
    },
    {
     "id": "UX-A3",
     "severity": "Medium",
     "confidence": 80,
     "element": "Users section hint: '1 of 2 seats used. Extra seats are add-ons \u2014 adjust them in Change plan & add-ons' (link reopens the drawer)",
     "finding": "The seat count is only editable inside the billing drawer, while the roster that tells you whether you need another seat is a separate section further down the page.",
     "user_impact": "A user deciding to add a teammate has to leave the roster, open the drawer, set a number without the roster visible, then come back to invite \u2014 the KF-2 adjacency failure.",
     "suggested_fix": "Put the seat stepper in the Users section itself and let it feed the same pending-total mechanism the drawer uses."
    },
    {
     "id": "UX-A4",
     "severity": "Medium",
     "confidence": 74,
     "element": "'Change plan & add-ons' as the single `btn-primary` in the Plan card, alongside 'Reconnect' and 'Reconnect QuickBooks' also styled `btn-primary`",
     "finding": "Three primary-weight buttons compete down the page, two of them recovery actions for broken connections, so the page has no single strongest call to action.",
     "user_impact": "On a page whose stated job is 'see what you pay for and change it', the change action carries the same weight as two error-recovery buttons.",
     "suggested_fix": "Demote the in-card Reconnect buttons to outlined, letting the top-of-page error alert carry the primary recovery CTA."
    }
   ]
  },
  {
   "prototype": "B",
   "findings": [
    {
     "id": "UX-B1",
     "severity": "Critical",
     "confidence": 92,
     "element": "`<div class=\"alert alert-error\">2 connections need attention. Sales data isn't reaching your books.</div>` at line 310, inside `<div class=\"pane\" id=\"org\">`, while `<div class=\"pane on\" id=\"bill\">` is the default tab",
     "finding": "The page's most urgent message \u2014 that sales data is not reaching the books \u2014 is inside the Organization pane and is invisible on load because Billing is the selected tab.",
     "user_impact": "A user can open this screen, change their plan, confirm a charge and leave without ever learning that two integrations are broken and no data is syncing.",
     "suggested_fix": "Hoist the aggregate error above the tab strip so it renders regardless of which tab is active, and badge the Organization tab with the issue count."
    },
    {
     "id": "UX-B2",
     "severity": "High",
     "confidence": 86,
     "element": "Quantity steppers rendered only in the currently selected plan column of `table.matrix`",
     "finding": "Because steppers exist only in the selected column, a user cannot see what their own quantities would cost under a different plan without first committing to selecting that plan.",
     "user_impact": "The comparison the matrix exists to support \u2014 'which plan is cheaper for my volume' \u2014 is the one comparison it cannot show; users select, read, deselect, and loop.",
     "suggested_fix": "Render steppers in all three columns bound to one shared quantity value, so changing a number reprices every plan simultaneously."
    },
    {
     "id": "UX-B3",
     "severity": "High",
     "confidence": 84,
     "element": "`table.matrix` \u2014 4 quantity rows plus 24 rows from `const FEATURES` (28 rows \u00d7 3 plan columns) with a sticky `thead`",
     "finding": "Four rows that change the bill and twenty-four static feature-comparison rows share one uniform table with identical row treatment and no grouping separation.",
     "user_impact": "The controls that cost money are visually indistinguishable from read-only marketing rows, so users scroll a 28-row grid hunting for the four things they can act on.",
     "suggested_fix": "Split the table: a short 'what you're buying' block with the four steppers above, and the feature comparison collapsed behind a 'Compare all features' disclosure."
    },
    {
     "id": "UX-B4",
     "severity": "Medium",
     "confidence": 80,
     "element": "Organization tab hint: '1 of 2 seats used. Seats are a billing add-on \u2014 change the count on the Billing tab' with a cross-tab link",
     "finding": "Buying a seat and assigning it are on two different tabs, so one job requires a tab round trip with the sticky confirm bar in between.",
     "user_impact": "The user leaves the roster, buys a seat, confirms a charge on another tab, then has to navigate back to invite anyone \u2014 and the sticky bar's Reset button sits right next to Confirm during that trip.",
     "suggested_fix": "Expose the seat stepper inline in the roster section, feeding the same sticky change bar."
    }
   ]
  },
  {
   "prototype": "C",
   "findings": [
    {
     "id": "UX-C1",
     "severity": "High",
     "confidence": 87,
     "element": "9 read-only `.scard` elements, each `onclick=\"openPanel(...)\"`, with the plan panel containing a 3-step Plan/Quantities/Review wizard",
     "finding": "Nothing on the overview is actionable \u2014 every task is at least one level deep, and changing a transaction quantity costs card \u2192 panel \u2192 3 wizard steps \u2192 header cart \u2192 confirm.",
     "user_impact": "The most common task on a billing screen (nudge a quantity) takes five-plus interactions, where B and v2 do it in one, so users bail or re-enter the panel repeatedly.",
     "suggested_fix": "Put the quantity steppers directly on the plan card and reserve the panel for the plan switch itself; drop the wizard to a single scrollable panel."
    },
    {
     "id": "UX-C2",
     "severity": "High",
     "confidence": 84,
     "element": "`#cartBtn` / `#cartBody` 'Pending changes' cart in the page header, while edits are made in a 620px side panel",
     "finding": "Paid changes accumulate in a header cart button far from the panel where the user is working, and closing the panel gives no in-context prompt to commit them.",
     "user_impact": "Users make a change, close the panel, see the page apparently unchanged, and leave \u2014 the queued charge never happens, or worse, is discovered on a later visit. This is the KF-5 hidden-menu pattern.",
     "suggested_fix": "Replace the header cart with a sticky bottom bar listing line items and total, as B and v2 do; contextual bars beat header popovers."
    },
    {
     "id": "UX-C3",
     "severity": "Medium",
     "confidence": 81,
     "element": "Preference panels (notifications, autocharge) save instantly; plan/quantity/add-on panels queue into the cart",
     "finding": "Two commit models are split across nine visually identical cards with nothing on the card face indicating which kind it is.",
     "user_impact": "The user cannot predict, before opening a card, whether their change will apply immediately or wait behind a Confirm \u2014 so they either double-confirm or assume something saved that didn't.",
     "suggested_fix": "Mark cards that hold billable settings with a persistent affordance (a price chip or '$' marker) so the two classes are separable at overview level."
    },
    {
     "id": "UX-C4",
     "severity": "Medium",
     "confidence": 76,
     "element": "Plan panel 3-step wizard: Plan / Quantities / Review inside a 620px panel",
     "finding": "A wizard sequences three views for what is a single interdependent decision \u2014 plan choice and quantities determine each other's price.",
     "user_impact": "Users must reach step 3 to see the total, then step back to step 1 to test a cheaper plan, looping through the wizard once per hypothesis.",
     "suggested_fix": "Collapse the wizard into one scrolling panel with a live total pinned at the panel footer."
    }
   ]
  },
  {
   "prototype": "v2",
   "findings": [
    {
     "id": "UX-v2-1",
     "severity": "High",
     "confidence": 85,
     "element": "`<details class=\"cmp\" id=\"cmpBox\"><summary>Compare plans & switch</summary>`, reachable only via a `btn-outlined` 'Compare plans' button in `#planActions`",
     "finding": "On a page titled 'Manage subscription', switching plan lives inside a collapsed disclosure and is reached through the page's only secondary-weight button, while quantity steppers sit exposed above it.",
     "user_impact": "Visual weight is inverted against task priority: the cheap, reversible action (nudge a quantity) is one click, and the page's headline action (change plan) requires discovering a closed accordion.",
     "suggested_fix": "Surface the three plan cards expanded by default, or promote the plan-switch trigger to primary weight in the plan header."
    },
    {
     "id": "UX-v2-2",
     "severity": "Medium",
     "confidence": 82,
     "element": "Blocked downgrade: `alert-error` guardrails render inline in the add-on list; the disabled CTA `go.textContent = 'Resolve the issue above'` sits in the fixed `.totalbar` at the bottom of the viewport",
     "finding": "The button that says 'Resolve the issue above' is pinned to the bottom of the screen, and the issue it points at can be scrolled anywhere above it, including off-screen.",
     "user_impact": "A user who scrolls to the total bar to commit sees only a dead button and a vague instruction, with no way to jump to the blocker it references.",
     "suggested_fix": "Make the bar text name the specific blocker and act as a scroll-to-anchor link into the offending guardrail alert."
    },
    {
     "id": "UX-v2-3",
     "severity": "Medium",
     "confidence": 74,
     "element": "Cancel flow: 3-step wizard gated on choosing one of 8 reasons before reaching the cancel action",
     "finding": "Cancellation requires selecting from an eight-item reason list before the user can proceed, and the retention offer shown depends on which reason was picked.",
     "user_impact": "Users whose actual reason isn't in the list pick arbitrarily and are shown an irrelevant offer, then abandon mid-wizard in an ambiguous subscription state.",
     "suggested_fix": "Make the reason step skippable ('Prefer not to say') and keep a state indicator visible so an abandoned wizard clearly leaves the subscription untouched."
    }
   ]
  },
  {
   "prototype": "Concepts 3-in-1",
   "findings": [
    {
     "id": "UX-5-1",
     "severity": "High",
     "confidence": 83,
     "element": "`.pill` switcher \u2014 'V1 \u00b7 The Ledger', 'V2 \u00b7 Calm Overview', 'V3 \u00b7 Unified Grid' \u2014 showing one concept at a time",
     "finding": "The page exists to compare three concepts but renders them mutually exclusive, so comparison is done from memory across pill switches.",
     "user_impact": "Reviewers form an impression of whichever concept they saw last rather than a real comparison, which is the opposite of what a concept-comparison artefact should produce.",
     "suggested_fix": "Offer a side-by-side or two-up mode so at least two concepts can be held in view together."
    },
    {
     "id": "UX-5-2",
     "severity": "High",
     "confidence": 86,
     "element": "`<button class=\"btn btn-secondary\" disabled>Update plan</button>`",
     "finding": "The commit action for a paid plan change is rendered secondary and disabled, giving it less visual weight than the ordinary 'Change'/'Subscribe' text links scattered through the breakdown above it.",
     "user_impact": "Users who have made a change look for the strongest control on screen to complete it and find text links instead, then can't tell whether the greyed button means 'nothing to save' or 'you're not allowed'.",
     "suggested_fix": "Make it a primary button and replace the bare disabled state with explicit text ('No changes to save')."
    },
    {
     "id": "UX-5-3",
     "severity": "Medium",
     "confidence": 84,
     "element": "V1 price-breakdown table row actions: 'Change plan', 'Change quantity', 'Add', 'Change', 'Manage roster'",
     "finding": "Five different action labels appear in one table for what is structurally the same operation \u2014 adjust a billable line item.",
     "user_impact": "Users can't build a rule for what a row action does, so each row has to be read individually and 'Add' vs 'Change' vs 'Subscribe' looks like a meaningful distinction when it isn't.",
     "suggested_fix": "Use one verb for the whole column ('Change'), with the not-subscribed state carried by the row's price cell rather than the button label."
    }
   ]
  },
  {
   "prototype": "V1 Unified Settings",
   "findings": [
    {
     "id": "UX-6-1",
     "severity": "High",
     "confidence": 88,
     "element": "Nine `btn-primary` buttons on one page: 'Apply changes', 'Subscribe' \u00d72, 'Purchase', 'Reconnect' \u00d72, 'Apply', 'Save changes', 'Update'",
     "finding": "Nine primary-weight buttons compete on a single scroll, so no action is visually primary and the page has no focal point.",
     "user_impact": "Users scanning for 'the button that does the thing I came for' get nine equally loud candidates and have to read every one.",
     "suggested_fix": "Allow one primary per page region at most, and demote per-row actions (Reconnect, Purchase, Apply) to outlined."
    },
    {
     "id": "UX-6-2",
     "severity": "High",
     "confidence": 85,
     "element": "`<button class=\"btn btn-primary btn-xs\" id=\"applyChanges\">Apply changes</button>` versus add-on `<button class=\"btn btn-primary btn-sm\">Subscribe</button>`",
     "finding": "The button that commits a change to the user's recurring bill is the smallest size in the system, while the add-on upsell buttons are larger.",
     "user_impact": "The most consequential control is the hardest to hit and the easiest to miss after adjusting a stepper, so quantity changes get set and never applied.",
     "suggested_fix": "Promote 'Apply changes' to the standard button size and move it into a sticky bar that appears when quantities are dirty."
    },
    {
     "id": "UX-6-3",
     "severity": "High",
     "confidence": 80,
     "element": "`<button class=\"btn btn-outlined btn-sm\">Upgrade plan</button>` above three `btn-primary` add-on 'Subscribe'/'Purchase' buttons",
     "finding": "Changing plan \u2014 the most likely reason a user opens this screen \u2014 is the only outlined control in a region where three add-on upsells are primary.",
     "user_impact": "Users hunting for the plan change are drawn to the louder add-on buttons first and may buy an add-on when a plan change was the right answer.",
     "suggested_fix": "Invert the weighting: plan change primary, add-on subscribes outlined."
    },
    {
     "id": "UX-6-4",
     "severity": "Medium",
     "confidence": 82,
     "element": "Two `\u22ee` `menu-btn` dropdowns (`intMenuDropdown`, `userMenuDropdown`) holding 'Refresh timezone', 'Disconnect', 'Delete'",
     "finding": "Destructive per-row actions are hidden behind kebab menus \u2014 the exact pattern LogRocket records as unresponsive in production (KF-5).",
     "user_impact": "Users click the kebab, get nothing they can perceive, and rage-click; the actions inside are also invisible to anyone scanning for them.",
     "suggested_fix": "Surface Disconnect/Delete as inline row actions or a contextual bar on row selection, not a hidden menu."
    }
   ]
  },
  {
   "prototype": "V2 Billing Hub",
   "findings": [
    {
     "id": "UX-7-1",
     "severity": "Critical",
     "confidence": 90,
     "element": "`.tier-steps` row \u2014 Starter / Essential / Medium / Scale / Enterprise as `.tier-step` divs with `.tier-dot` and `.tier-connector done|current`, zero onclick handlers on any of the 6 steps",
     "finding": "The five plan names are rendered as a progress stepper with dots, connectors and a 'current' state, but nothing in it is clickable \u2014 it looks exactly like a plan selector and is pure decoration.",
     "user_impact": "This is KF-4 verbatim: stepper labels are the densest dead-click zone in production, and here the dead labels are the plan names on the screen whose whole job is changing plan.",
     "suggested_fix": "Make each tier step a real selector that repriced the summary, or remove the dots and connectors so it stops reading as an interactive tier picker."
    },
    {
     "id": "UX-7-2",
     "severity": "Critical",
     "confidence": 87,
     "element": "Line 607: `<button class=\"btn btn-primary\" onclick=\"switchTab('billing')\">Upgrade plan \u2192</button>`, inside `#tab-billing`, which is the default active tab",
     "finding": "The page's loudest CTA switches to the tab it is already on, so clicking it produces no observable change.",
     "user_impact": "Combined with the inert tier stepper, a user who wants a different plan has no working path to one \u2014 the primary task is not completable from this screen.",
     "suggested_fix": "Point 'Upgrade plan' at the plan-selection region (scroll + focus) or remove it, since the quantity block already carries its own Update plan."
    },
    {
     "id": "UX-7-3",
     "severity": "High",
     "confidence": 86,
     "element": "Two `btn-primary btn-sm` buttons both labelled 'Update plan' at lines 768 and 776, the second carrying `btn-disabled` and `id=\"updatePlanBtn\"`",
     "finding": "Two identically labelled primary buttons sit eight lines apart, one live and one permanently disabled.",
     "user_impact": "Users cannot tell which 'Update plan' is theirs; clicking the disabled one reads as the app being broken rather than as 'no changes yet'.",
     "suggested_fix": "Keep one commit button whose state reflects dirtiness, and give the disabled state explicit text."
    },
    {
     "id": "UX-7-4",
     "severity": "Medium",
     "confidence": 82,
     "element": "Three `dot-menu-btn` kebabs (`intMenu`, `userMenu1`, `userMenu2`) holding 'Refresh timezone', 'Disconnect', 'Delete'",
     "finding": "Destructive row actions are behind three hidden kebab menus, the KF-5 pattern.",
     "user_impact": "Same dead-click and rage-click behaviour production already exhibits, on Disconnect and Delete specifically.",
     "suggested_fix": "Replace kebabs with inline row actions or a selection-driven contextual bar."
    }
   ]
  },
  {
   "prototype": "V3 Org Health Dashboard",
   "findings": [
    {
     "id": "UX-8-1",
     "severity": "High",
     "confidence": 84,
     "element": "Four `.status-card` tiles (Subscription / Integrations / Accounting / Team) restating plan+price+renewal, integration status, accounting status and seat count, all of which repeat verbatim in the sections below",
     "finding": "Every fact in the four-card header row appears again in full a screen below it, so the page states the same four things twice with equal visual weight.",
     "user_impact": "Users read the cards, scroll, encounter the same information, and can't tell whether the lower section is the same data or an update \u2014 doubling the scan cost of the page.",
     "suggested_fix": "Let the cards be pure status and strip the duplicated detail from them, or make the sections load only on card activation."
    },
    {
     "id": "UX-8-2",
     "severity": "High",
     "confidence": 83,
     "element": "Top `<button class=\"btn btn-primary\" onclick=\"showToast('Redirecting to upgrade flow\u2026')\">Upgrade plan</button>` versus inline `stepChange('tx', \u00b1500)` steppers with their own 'Update plan' button",
     "finding": "Two contradictory models for changing what you pay coexist on one screen: one redirects out to a separate flow, the other edits in place.",
     "user_impact": "A user who wants more transactions can't tell whether to click Upgrade plan (leaving the page and losing context) or use the steppers, and the two paths may not agree on the resulting price.",
     "suggested_fix": "Pick one \u2014 keep the inline configurator and delete the redirect, or make the top button scroll to the configurator instead of navigating away."
    },
    {
     "id": "UX-8-3",
     "severity": "Medium",
     "confidence": 78,
     "element": "Five `.notif-row` toggles (activity digest, reconciliation reminder, failed sync alerts, new member, billing notifications) with a bare `.notif-control` and no save affordance or confirmation element",
     "finding": "The notification toggles have neither a save button nor any '\u2713 Saved' confirmation, unlike the sibling prototypes which all show one.",
     "user_impact": "KF-6 territory: with no visible acknowledgement the user cannot tell the preference took, so they toggle back and forth to test it.",
     "suggested_fix": "Emit the same inline '\u2713 Saved' confirmation the other prototypes use, adjacent to the toggle rather than as a corner toast."
    },
    {
     "id": "UX-8-4",
     "severity": "Medium",
     "confidence": 76,
     "element": "`stepChange('tx', -500)` / `stepChange('tx', 500)` \u2014 transactions move only in blocks of 500, with no text entry",
     "finding": "The transaction quantity can only be changed in 500-unit jumps with no way to type a target number.",
     "user_impact": "A user who knows they need 1,200 transactions must click to 1,500 and overbuy, or click repeatedly to approximate \u2014 and the increment isn't labelled anywhere near the control.",
     "suggested_fix": "Make the value an editable field with the steppers as a convenience, and label the block size next to it."
    }
   ]
  },
  {
   "prototype": "Sketch V1 Billing+Org",
   "findings": [
    {
     "id": "UX-9-1",
     "severity": "High",
     "confidence": 85,
     "element": "Subscription table \u2014 7 rows (Pro plan, Smart rules, Invoicing, RevRec, Sales volume, JS modes, Users), each with 'Edit' as its only action, and no total row",
     "finding": "The table lists seven billable lines with an Edit link each and never totals them, so there is no figure on the page for what the subscription costs.",
     "user_impact": "Users must open seven separate editors and add up the results to answer the single question the screen should answer on load \u2014 what am I paying.",
     "suggested_fix": "Add a total row and show a running new-total as soon as any row is edited."
    },
    {
     "id": "UX-9-2",
     "severity": "High",
     "confidence": 82,
     "element": "All 7 table rows display the identical '20%' discount and '95 USD/mo' price; the Details column mixes '\u2014', '3 / 10', '40 / 100', '800 / 1,000', '10,000 / 20,000', '2 / 3'",
     "finding": "Every row carries the same price and discount, so nothing in the table distinguishes the plan from an add-on or one add-on's cost from another's, while the Details column silently switches units row to row.",
     "user_impact": "The table gives no usable price signal at all \u2014 users can't rank what's expensive, and the mixed 'used / limit' units make the plan row's '\u2014' read as missing data.",
     "suggested_fix": "Show real per-line prices and label the Details column's unit per row (e.g. '3 of 10 rules')."
    },
    {
     "id": "UX-9-3",
     "severity": "High",
     "confidence": 84,
     "element": "Table row 1 reads 'Pro plan'; the Subscription block below reads 'Plan: Medium, yearly \u2014 Canceled \u00b7 Active till Mar 10, 2024'",
     "finding": "The same screen states two different plans and a cancelled status against an already-past expiry date, in two places about a screen apart.",
     "user_impact": "The user cannot determine which plan they are on or whether the subscription is live \u2014 the screen contradicts itself on its single most important fact.",
     "suggested_fix": "Derive both regions from one source of truth and surface cancellation as a page-level banner rather than a field buried in a second block."
    },
    {
     "id": "UX-9-4",
     "severity": "Medium",
     "confidence": 79,
     "element": "'Setting name \u24d8' rendered as the literal visible label above 'Organization name', 'Time zone', 'Notify about the app activity' and 'Reconciliation reminder'",
     "finding": "Four form controls carry 'Setting name \u24d8' as their label with the actual name rendered as secondary text beneath, so the label position holds no information.",
     "user_impact": "Users scanning labels see the same placeholder four times and must read the second line of each row to identify any field, defeating the label hierarchy.",
     "suggested_fix": "Promote the real setting name into the label slot and attach the info icon to it."
    }
   ]
  },
  {
   "prototype": "Sketch V2 Tabbed",
   "findings": [
    {
     "id": "UX-10-1",
     "severity": "Critical",
     "confidence": 89,
     "element": "5 add-on cards (Monthly transactions, Invoicing, Smart rules, Revenue recognition, Historical transactions) \u2014 5 occurrences of the identical description 'Benefit from flat fee rather than commission on every invoice amount!' and 7 occurrences of '$0.00 USD'",
     "finding": "All five add-ons share one description that only makes sense for Invoicing, and every price reads $0.00.",
     "user_impact": "Users cannot tell the add-ons apart or what any of them costs, which makes the primary task \u2014 decide what to buy \u2014 impossible on this screen.",
     "suggested_fix": "Give each add-on its own one-line value statement and a real price, as the Round-2 prototypes do."
    },
    {
     "id": "UX-10-2",
     "severity": "High",
     "confidence": 83,
     "element": "Integrations table row: 'My wonderful flowers (Stripe) \u00b7 Connection lost since May 20, 2025' \u2014 zero occurrences of 'Reconnect' anywhere in the page",
     "finding": "The page reports a broken Stripe connection and offers no recovery action, in the row or anywhere else.",
     "user_impact": "The user learns their sales data has been failing to sync for months and has no next step from the place they learned it \u2014 the KF-2 failure where a step is reported but the required next action isn't adjacent.",
     "suggested_fix": "Put a Reconnect button in the row and mirror the count in a page-level alert."
    },
    {
     "id": "UX-10-3",
     "severity": "Medium",
     "confidence": 76,
     "element": "'Next billing date: 11/06/2021'",
     "finding": "The single figure telling the user when they'll next be charged is in an ambiguous all-numeric format that could be 11 June or November 6.",
     "user_impact": "Users in different locales read a different date from the same string on a screen where the date determines when money moves.",
     "suggested_fix": "Use an unambiguous format ('11 Jun 2027') consistently, matching the Round-2 prototypes."
    },
    {
     "id": "UX-10-4",
     "severity": "Medium",
     "confidence": 78,
     "element": "Quantity fields ('500') adjacent to '$0.00 USD' per add-on card, with no running total and no confirm control on the page",
     "finding": "Quantities are editable but there is no total anywhere and no commit button, so a change has no visible price consequence and no way to be applied.",
     "user_impact": "Users adjust numbers, see nothing change, and have no control to complete the purchase \u2014 the flow dead-ends.",
     "suggested_fix": "Add a sticky bar with line items, monthly delta and a confirm action."
    }
   ]
  },
  {
   "prototype": "Sketch V2 Long page",
   "findings": [
    {
     "id": "UX-11-1",
     "severity": "Critical",
     "confidence": 89,
     "element": "Addons section \u2014 same 5 cards, same single repeated description 'Benefit from flat fee rather than commission on every invoice amount!', all '$0.00 USD'",
     "finding": "Inherits the tabbed version's defect: five indistinguishable add-ons with no real prices.",
     "user_impact": "The buying decision this section exists to support cannot be made \u2014 nothing differentiates the options or reveals cost.",
     "suggested_fix": "Per-add-on copy and real prices, with the quantity-bearing add-ons visually separated from the flat-fee ones."
    },
    {
     "id": "UX-11-2",
     "severity": "High",
     "confidence": 80,
     "element": "`#anchorNav` with 5 `.anchor-link` entries: Connections, Plan and billing, Users, Organization name, Notifications \u2014 no entry for Addons",
     "finding": "The in-page anchor nav omits Addons, the only section that changes the recurring bill; it's reachable only by scrolling past 'Plan and billing'.",
     "user_impact": "On a long page the nav is the primary wayfinding device, so the section that costs money is the one section users can't jump to \u2014 and its absence implies it's part of 'Plan and billing' when it's separate.",
     "suggested_fix": "Add Addons to the anchor nav, or nest it visibly under Plan and billing so the nav's structure matches the page's."
    },
    {
     "id": "UX-11-3",
     "severity": "High",
     "confidence": 82,
     "element": "Connections section: 'My wonderful flowers (Stripe) \u00b7 Connection lost since May 20, 2025' with no Reconnect action",
     "finding": "Same as the tabbed variant \u2014 a long-running sync failure is reported with no recovery path adjacent to it.",
     "user_impact": "Users see the problem at the top of the page and have nowhere to act on it, so they scroll the whole page looking for a fix that isn't there.",
     "suggested_fix": "Inline Reconnect in the row plus a page-level alert above the anchor nav."
    },
    {
     "id": "UX-11-4",
     "severity": "Medium",
     "confidence": 75,
     "element": "'Organization name' used as both the section heading and a field label inside it, with 'Setting name ?' as the visible label between them",
     "finding": "The same string labels a section and a field one level down, and the placeholder label 'Setting name ?' sits between them.",
     "user_impact": "Users can't tell whether the section covers one field or several, and the nav entry 'Organization name' lands on a heading that looks like the field itself.",
     "suggested_fix": "Rename the section to cover its actual contents (e.g. 'Organization details') and put real names in the label slots."
    }
   ]
  }
 ],
 "ranking": [
  {
   "rank": 1,
   "prototype": "v2",
   "why": "One surface, one commit path, a persistent total bar, and the only prototype that blocks a destructive downgrade with named consequences and one-click fixes instead of letting the user discover the damage after paying."
  },
  {
   "rank": 2,
   "prototype": "A",
   "why": "A single readable page with one dominant CTA and a live total in the drawer footer; its problems are the three coexisting save models and the inability to see old and new cost together, both fixable without changing the shape."
  },
  {
   "rank": 3,
   "prototype": "C",
   "why": "The clearest scan of the eleven and the only one whose card grid is properly keyboard-operable, but every task is a level deep and the header cart puts the commit action away from the work."
  },
  {
   "rank": 4,
   "prototype": "B",
   "why": "The sticky change bar with per-line items and monthly delta is the best commit affordance in the set, but a 28-row matrix and a critical error hidden behind an unselected tab cost it more than the bar earns."
  },
  {
   "rank": 5,
   "prototype": "Concepts 3-in-1",
   "why": "The pill switcher and the itemised breakdown with plain-language guarantees are strong, but the commit button is secondary and disabled and the three concepts can never be seen together."
  },
  {
   "rank": 6,
   "prototype": "V3 Org Health Dashboard",
   "why": "Clickable status cards give real triage value, undercut by stating every fact twice on one page and offering two contradictory ways to change what you pay."
  },
  {
   "rank": 7,
   "prototype": "V1 Unified Settings",
   "why": "It has a live 'New total' preview next to the steppers, which most Round-1 prototypes lack, but nine competing primary buttons and an extra-small commit button leave the page with no focal point."
  },
  {
   "rank": 8,
   "prototype": "Sketch V2 Long page",
   "why": "The anchor nav makes a long page navigable, but it omits the one section that changes the bill and the add-on content is unusable."
  },
  {
   "rank": 9,
   "prototype": "Sketch V2 Tabbed",
   "why": "Same unusable add-on content as the long page without the anchor nav, and a months-old broken connection reported with no recovery action anywhere on the screen."
  },
  {
   "rank": 10,
   "prototype": "Sketch V1 Billing+Org",
   "why": "The screen contradicts itself on which plan the user is on and whether the subscription is live, and seven billable rows never total to a number."
  },
  {
   "rank": 11,
   "prototype": "V2 Billing Hub",
   "why": "The plan tier row is a decorative stepper with no click handlers and the primary 'Upgrade plan' button switches to the tab it is already on, so the primary task cannot be completed at all."
  }
 ],
 "verdict": "On usability alone v2 wins: it is the only prototype where the whole job lives on one surface with one commit path, and the only one that stops a user from downgrading into data loss by naming the specific consequences and offering fixes in place. Its single real defect is inverted hierarchy \u2014 the plan switch is buried in a collapsed 'Compare plans & switch' disclosure behind a secondary button while quantity steppers sit exposed, so the page's headline action is the hardest thing on it to find. For A to overtake it, the runner-up would need to collapse its three commit models into one and show current-versus-new cost together instead of hiding the page's breakdown behind the drawer scrim; B's sticky change bar is the best commit affordance in the set and worth porting into whichever direction ships, but B itself is disqualified by burying a critical sync-failure alert behind an unselected tab."
}