{
 "validator": "ux",
 "instance": 3,
 "per_prototype": [
  {
   "prototype": "A",
   "findings": [
    {
     "id": "UX-A1",
     "severity": "High",
     "confidence": 88,
     "element": "Four commit models on one page: instant-save notification toggles; sticky bottom savebar for Organization details; drawer footer \"Review & confirm\"; modals that charge on a single click",
     "finding": "One screen teaches four different rules for \"is my change saved / charged yet?\". Notifications commit on change (toast). Details commit via a page-level sticky savebar with Discard/Save. Plan and add-ons commit via a drawer footer with a review step. The historical-transactions and invite modals commit on the modal's own primary button with no review. The page copy asserts one model (\"Only paid changes and the details form ask you to confirm\") that the Buy modal contradicts.",
     "user_impact": "A repeat user cannot form a single mental rule, so on every visit they must re-derive which control is already live. That drives the two production failure modes: re-clicking a control that already saved, and walking away from a configured change that was never committed (KF-2).",
     "suggested_fix": "Pick one commit contract per consequence class and state it once: free preferences instant with inline confirmation, everything that costs money through the same review-then-confirm step (drawer footer), no exceptions in modals."
    },
    {
     "id": "UX-A2",
     "severity": "High",
     "confidence": 90,
     "element": "\"Buy historical transactions\" modal \u2014 stepper, one-time total, primary button \"Buy\"",
     "finding": "A real one-time charge ($30.00 per 1,000) commits on a single \"Buy\" click: no card named, no review step, no proration/tax line, no receipt, no undo \u2014 just a toast (\"Purchased. Historical balance updated.\"). Meanwhile a $12/mo seat change inside the drawer requires \"Review & confirm\". The commit weight is inverted relative to reversibility: the recurring, prorated, reversible change is gated; the irreversible one-time purchase is not.",
     "user_impact": "An accountant stepping the quantity and reflexively hitting the primary button buys credits they didn't intend, with no path back and no artefact to reconcile against.",
     "suggested_fix": "Route the purchase through the same review step as the drawer: show card, amount, tax note and the words \"non-refundable\", and label the button with the amount (\"Buy \u2014 $30.00\")."
    },
    {
     "id": "UX-A3",
     "severity": "Medium",
     "confidence": 80,
     "element": "Sticky savebar: \"<N> unsaved change in Organization details\" + Discard + Save changes",
     "finding": "The dirty counter increments per field touch and names no field. Tabbing through the eight address inputs reads \"8 unsaved changes\" with no list, and Discard is an all-or-nothing wipe \u2014 there is no per-field revert and no diff.",
     "user_impact": "An expert correcting one line of a billing address (which drives tax) cannot verify what else they perturbed, so the safe move is to discard everything and start again.",
     "suggested_fix": "Make the savebar expand to a field-level diff (was \u2192 now) with per-row revert; count changed values, not focus events."
    },
    {
     "id": "UX-A4",
     "severity": "Medium",
     "confidence": 78,
     "element": "On-page \"What you're paying for\" table vs. the drawer's expandable itemised breakdown",
     "finding": "The same itemisation exists twice in two places that can never be seen together: the page table shows today's numbers, the drawer shows the configured numbers, and the 720px drawer over a scrim covers the page table while you configure.",
     "user_impact": "The before/after comparison an accountant needs to justify a change is split across two layers; verifying it means opening and closing the drawer repeatedly, and the Plan & billing section alone is already ~2 viewports.",
     "suggested_fix": "Show now \u2192 after in one table inside the drawer (as v2 and C do) and drop the duplicate on the page, or narrow the drawer so the page total stays visible."
    },
    {
     "id": "UX-A5",
     "severity": "Medium",
     "confidence": 72,
     "element": "Drawer footer live total, and the disabled \"Review & confirm\" until something changes",
     "finding": "The footer surfaces the new monthly total but the delta versus what the org pays today is only reached at the confirm step, after the configuration work is done \u2014 the number that decides the change is the last one shown.",
     "user_impact": "Repeat users configure blind against today's price and discover the delta only at the end, which is where they abandon or re-open the drawer to re-check.",
     "suggested_fix": "Put \"$92.00 today \u2192 $104.00 (+$12.00/mo)\" in the drawer footer from the first stepper click, as v2's total bar does."
    }
   ]
  },
  {
   "prototype": "B",
   "findings": [
    {
     "id": "UX-B1",
     "severity": "High",
     "confidence": 88,
     "element": "Matrix configurator \u2014 quantity steppers rendered only in the selected plan's column; other columns render static \"1,000 included\"; Premium column renders \"negotiated\"",
     "finding": "The screen's entire premise is cross-plan comparison, but cost comparison at a chosen volume is impossible: steppers are interactive only in the selected column, and the Premium column never prices anything. To answer \"what would 5,000 transactions cost on Pro vs Premium?\" the user must commit to selecting a plan, then re-select the other, and Premium yields no number at all.",
     "user_impact": "The one question a repeat buyer brings to a comparison matrix is the one it cannot answer, so the decision moves off-screen (spreadsheet, sales call) \u2014 a false affordance at the scale of the whole layout.",
     "suggested_fix": "Price every column at the current quantities simultaneously; keep one shared quantity row driving all three columns, with per-column totals."
    },
    {
     "id": "UX-B2",
     "severity": "High",
     "confidence": 85,
     "element": "~28-row \u00d7 4-column table: 4 quantity rows then 24 static feature rows, ~200px sticky header held for the whole scroll, plus fixed bottom total bar",
     "finding": "Roughly 300px of the 900px viewport is permanent chrome, leaving ~600px of rows for a 28-row table. The only interactive rows (the four quantities) sit at the top, and the 24 static feature rows below exist solely for a first-time plan decision \u2014 yet they are re-scrolled on every visit.",
     "user_impact": "For the monthly \"add 500 transactions\" task, the expert pays the full cost of a marketing comparison table every time; volume and scrolling become the task.",
     "suggested_fix": "Split the two jobs: quantities as a compact always-visible configurator, the 24-row feature comparison behind \"Compare plans\" (v2's collapsed pattern)."
    },
    {
     "id": "UX-B3",
     "severity": "Medium",
     "confidence": 82,
     "element": "People card on the Organization tab: \"Seats are a billing add-on \u2014 change the count on the Billing tab\"",
     "finding": "Seat quantity lives in the Billing matrix while the roster lives on the Organization tab. The two halves of one task are on opposite sides of a top-level tab switch, and the pointer is a sentence rather than a control.",
     "user_impact": "Adding a teammate becomes: read hint \u2192 switch tab \u2192 find the right row in a 28-row matrix \u2192 commit \u2192 switch back \u2192 invite. The roster context is lost mid-task.",
     "suggested_fix": "Put an inline \"Add a seat (+$12/mo)\" control in the People card that opens the same confirm as the matrix; keep the user on the roster."
    },
    {
     "id": "UX-B4",
     "severity": "Medium",
     "confidence": 80,
     "element": "pick() \u2014 selecting a plan silently resets each quantity to max(requested, new plan's included)",
     "finding": "Choosing a different plan column moves quantities the user never touched. The only record is the small caption under each stepper flipping between \"included\" and \"+$x/mo\"; nothing announces that a quantity was raised or that a previously requested figure was overridden by a plan minimum.",
     "user_impact": "An expert who set 5,000 transactions, then compared plans, cannot tell whether the number on screen is theirs or the plan's \u2014 and the sticky total moves for reasons that aren't attributable.",
     "suggested_fix": "Flag changed cells explicitly (\"raised to 3,000 \u2014 Pro's minimum\") and keep the user's requested figure visible alongside."
    },
    {
     "id": "UX-B5",
     "severity": "Medium",
     "confidence": 74,
     "element": "26px stepper buttons inside matrix cells, adjacent to 24 rows of non-interactive \u2713/\u2014/text cells",
     "finding": "Interactive controls are 26px targets embedded in a grid whose other 24 rows are visually identical cells that do nothing. KF-4 already records stepper labels as Synder's densest dead-click zone; this layout multiplies the look-alike-but-dead surface around them.",
     "user_impact": "Mis-clicks land on inert cells with no feedback, which is the pattern that produces repeat clicking and reload hammering (KF-1).",
     "suggested_fix": "Enlarge stepper targets to \u226540px, and give interactive rows a distinct background so the live band of the table is unmistakable."
    }
   ]
  },
  {
   "prototype": "C",
   "findings": [
    {
     "id": "UX-C1",
     "severity": "High",
     "confidence": 88,
     "element": "addPlan()/addBalance()/addDetails() call closeAll(); the only persistent trace is a header cart badge; toast auto-dismisses after 4.4s",
     "finding": "Queueing a paid change closes the panel and returns the user to nine read-only cards. Nothing is charged until Confirm, but after the toast expires the required next action lives only in a header button and two small \"change pending\" captions \u2014 there is no sticky bar and no persistent prompt.",
     "user_impact": "This is KF-2 exactly: the next required action is neither obvious nor adjacent. A user who configured a plan change and got interrupted leaves believing it applied, because the flow ended with a success-shaped toast.",
     "suggested_fix": "Replace the header cart with a persistent bottom bar carrying the count, the $ delta and \"Review & confirm\" \u2014 the contextual-bar pattern KF-5 says beats hidden/remote controls."
    },
    {
     "id": "UX-C2",
     "severity": "High",
     "confidence": 82,
     "element": "Pending-changes cart mixing cart.plan / cart.balance / cart.details under one \"Confirm everything\"; footer shows the monthly delta and one-time charge",
     "finding": "A free edit and paid changes share one commit. Saving an organization-name or address change requires passing through a cart whose footer reads as a money commitment, and confirming a typo fix can simultaneously charge a card. The cart even labels itself \"Nothing has been charged or saved yet\" \u2014 one sentence covering two different kinds of consequence.",
     "user_impact": "Users hesitate on a free action because it is dressed as a purchase, and \u2014 worse \u2014 a user who only meant to save an address confirms a queued charge alongside it. There is no way to commit one without the other.",
     "suggested_fix": "Let free settings save on their own (as notifications already do) and reserve the cart for anything that moves money; if both are pending, confirm them separately with distinct buttons."
    },
    {
     "id": "UX-C3",
     "severity": "Medium",
     "confidence": 82,
     "element": "Sync-balance panel: autocharge toggle commits instantly via toast, while \"Buy historical transactions\" beside it queues into the cart",
     "finding": "Two commit models sit inside a single 620px panel, and both affect money. The toggle that authorises automatic top-up charges is the instant one; the explicit purchase is the deferred one.",
     "user_impact": "The higher-consequence control (a standing authorisation to charge the card) has the lighter commit. Within one panel the user cannot predict which control is already live.",
     "suggested_fix": "Queue the autocharge authorisation in the cart too, or at minimum give it an explicit confirm naming the card and cap."
    },
    {
     "id": "UX-C4",
     "severity": "Medium",
     "confidence": 85,
     "element": "9 read-only status cards; every mutation routed through a 620px side panel; plan panel is a 3-step wizard",
     "finding": "No value is editable in place. The cheapest possible edit costs card \u2192 panel \u2192 (step 1/2/3) \u2192 add to cart \u2192 find header cart \u2192 confirm, and the panel closes on every queue action, so consecutive edits restart the sequence.",
     "user_impact": "Highest per-field click cost of the eleven. For an accountant running this monthly across many client orgs, the read-only-card discipline that makes C safe for a first-timer is the thing that makes it slow for a professional.",
     "suggested_fix": "Keep panels for multi-field flows but allow inline editing of single scalars (seat count, quantities) directly on the cards, feeding the same cart."
    },
    {
     "id": "UX-C5",
     "severity": "Medium",
     "confidence": 75,
     "element": "cart = { plan, details, balance } \u2014 one slot per category, overwritten on re-queue",
     "finding": "A second pass at the plan silently replaces the first; the review screen shows only the final now \u2192 after diff. Nothing signals that an earlier queued configuration was discarded.",
     "user_impact": "An expert who iterates (a normal way to price a change) cannot confirm which version is about to be charged, and \"Discard all\" is the only correction \u2014 losing the free edits queued alongside.",
     "suggested_fix": "Show queued items as a revisable list with per-item edit and remove, and note when a re-queue replaces a prior entry."
    }
   ]
  },
  {
   "prototype": "v2",
   "findings": [
    {
     "id": "UX-v2-1",
     "severity": "High",
     "confidence": 85,
     "element": "guardrails() \u2014 Smart rules block: \"You have 3 smart rules configured but you'd be entitled to 1\", fix offers only \"Keep them\"",
     "finding": "This is a hard block (Confirm becomes disabled \"Resolve the issue above\") whose single offered remedy undoes the downgrade. The Users block, by contrast, offers both \"Keep N seats\" and \"Remove someone first\". There is no link to delete rules, so from this screen an org with more rules than the target plan allows cannot complete a downgrade at all.",
     "user_impact": "The user's actual goal \u2014 spend less \u2014 is unreachable, and the interface never says where to go. Task failure with no exit, on the exact path (downgrade) that decides whether they cancel instead.",
     "suggested_fix": "Mirror the Users pattern: add \"Remove rules first \u2192\" deep-linking to Smart rules, and state how many must go (\"delete 2 of 3\")."
    },
    {
     "id": "UX-v2-2",
     "severity": "Medium",
     "confidence": 85,
     "element": "Blocks and warnings computed only in openConfirm(); page steppers and the fixed total bar show no advance signal",
     "finding": "The page happily prices an impossible configuration. The total bar shows a valid-looking amount and an enabled \"Review change\", and only the confirm dialog reveals that the change is blocked.",
     "user_impact": "The expert completes the whole configuration before learning it can't happen \u2014 the most expensive point at which to be told. On the pro3 scenario this is the default outcome of any downgrade attempt.",
     "suggested_fix": "Evaluate guardrails live: mark the offending stepper as it crosses the line and reflect it in the total bar (\"blocked \u2014 3 people use 2 seats\") before the review step."
    },
    {
     "id": "UX-v2-3",
     "severity": "Medium",
     "confidence": 76,
     "element": "Guardrail fix \"Remove someone first\" \u2192 closeM('mConfirm') + toast, navigating away from a configured change",
     "finding": "The escape hatch closes the confirm and sends the user elsewhere without stating that their configuration is held. In the prototype sel survives in memory; nothing on screen promises that, and in a real app a route change would not.",
     "user_impact": "A user who leaves to free a seat has no reason to believe the work is waiting, so the rational move is to abandon and start over \u2014 repeated on every monthly downgrade.",
     "suggested_fix": "Say it explicitly (\"we'll keep this change waiting\") and return to the confirm dialog with the same configuration when the blocker clears."
    },
    {
     "id": "UX-v2-4",
     "severity": "Medium",
     "confidence": 78,
     "element": "Cancel wizard: 3 steps, 8 reasons \u00d7 up to 4 sub-reasons, a per-reason retention offer, then confirm",
     "finding": "Every cancellation runs the full reason taxonomy and a retention pitch. There is no path for a user who has already decided, and no distinction between a one-off churn and an accounting firm routinely closing finished client orgs.",
     "user_impact": "For the repeat operator the retention step is pure friction re-paid each time; the well-designed offers land on someone who cannot act on them and will read the whole flow as an obstacle course.",
     "suggested_fix": "Keep the reason capture (it's valuable) but make the retention offer skippable in one click, and detect firm-managed orgs to drop the offer entirely."
    },
    {
     "id": "UX-v2-5",
     "severity": "Medium",
     "confidence": 72,
     "element": "Three-column plan comparison inside a collapsed <details> (\"Compare plans & switch\"), on a page whose fixed total bar always drives toward a plan/quantity decision",
     "finding": "The highest-stakes decision on the page \u2014 which plan \u2014 is the most hidden element, while quantity steppers (lower stakes, reversible) are always visible. The upgrade CTA scrolls to add-ons rather than opening the comparison.",
     "user_impact": "Users configuring quantities can overpay for overage rather than move up a plan, because the cheaper structural option is behind a disclosure they never open.",
     "suggested_fix": "When configured overage approaches the next plan's price, surface the comparison inline with a direct \"Pro is cheaper at this volume\" prompt."
    }
   ]
  },
  {
   "prototype": "Concepts 3-in-1",
   "findings": [
    {
     "id": "UX-CON-1",
     "severity": "High",
     "confidence": 90,
     "element": "All three variants: every action is href=\"#\" \u2014 Change plan, Change quantity, Subscribe, Buy, Top up, Adjust, Manage roster, Start cancellation\u2026",
     "finding": "The only script on the page is the V1/V2/V3 pill switcher. Nothing inside any variant responds, so there is no commit moment, no total that moves, no confirm, no error state \u2014 the part of a billing screen where users actually lose money is entirely unbuilt.",
     "user_impact": "The three concepts can only be judged on layout. The decision this artefact is meant to inform \u2014 how a paid change is committed and reversed \u2014 cannot be evaluated from it.",
     "suggested_fix": "Wire one end-to-end paid change per variant (quantity \u2192 live total \u2192 confirm with amount \u2192 result), even if faked; layout without the commit path can't be compared."
    },
    {
     "id": "UX-CON-2",
     "severity": "High",
     "confidence": 85,
     "element": "V1 The Ledger \u2014 price-breakdown table with six independent row actions: Change plan, Change quantity (RevRec), Change quantity (Invoicing), Add (Smart rules), Change (Transactions), Manage roster",
     "finding": "Each row owns its own entry point and there is no combined configurator or single commit. One intended subscription change (say: bigger plan, more seats, drop an add-on) becomes six separate flows, six confirmations and six proration events \u2014 and the table's Subtotal/Total rows are static, so nothing previews the combined result.",
     "user_impact": "The accountant-first variant makes the accountant's actual job \u2014 reason about the whole bill, change it once \u2014 impossible; and six sequential commits against one term produce a proration trail no one can reconcile.",
     "suggested_fix": "Keep the ledger as the read view but funnel every row action into one configurator with a single now \u2192 after total and one commit (the model A, B, C and v2 all converged on)."
    },
    {
     "id": "UX-CON-3",
     "severity": "High",
     "confidence": 85,
     "element": "V1 header: \"Next invoice \u00b7 Mar 13, 2027 / $241.00 / One invoice for plan + every add-on \u00b7 card \u2022\u2022\u2022\u2022 4242 \u00b7 billed yearly\" vs. breakdown row \"Monthly total $241.00\"",
     "finding": "The same figure is labelled both \"next invoice\" and \"monthly total\" on a yearly cycle, where the invoice would be roughly $2,892. The most consequential number on the screen has two incompatible readings and no annual figure anywhere.",
     "user_impact": "The user cannot tell what will hit the card on renewal day. For an accountant forecasting cash, an ambiguous invoice amount is worse than no amount \u2014 and it is the figure the page is built around.",
     "suggested_fix": "Show both explicitly, as V3 Dashboard does: \"$241.00/mo \u00b7 $2,892.00 charged 13 Mar 2027\"."
    },
    {
     "id": "UX-CON-4",
     "severity": "Medium",
     "confidence": 76,
     "element": "V2 Calm Overview \u2014 full-width reassurance block (\"You're all set through March 13, 2027\", usage bar, shield/flag notes) above the plan card; quantity controls followed by the caption \"Change a quantity above to enable\"",
     "finding": "Everything before the first control is reassurance the repeat user already has, and the commit control's availability is described in prose rather than shown \u2014 a disabled action explained by a sentence underneath it, with no price delta anywhere near the steppers.",
     "user_impact": "The monthly operator scrolls past comfort copy to reach work, then meets a control whose state they must read to understand. Reassurance for a first-timer becomes overhead for the expert.",
     "suggested_fix": "Collapse the reassurance to one line once the org is healthy, and put the live delta plus an enabled/disabled commit next to the steppers instead of describing it."
    },
    {
     "id": "UX-CON-5",
     "severity": "Medium",
     "confidence": 72,
     "element": "V3 Unified Grid \u2014 six identically-shaped add-on rows each with its own Change/Subscribe/Buy, and the itemisation behind \"See full breakdown\"",
     "finding": "The variant's virtue (one component pattern for everything) removes hierarchy: an active $79/mo add-on, an unsubscribed one and a one-time top-up look the same, and no view on the page itemises plan + add-ons without a second click.",
     "user_impact": "Scanning six uniform rows to establish what is currently paid for takes longer than reading V1's table; the uniformity that makes the grid extensible makes the current state hard to read at a glance.",
     "suggested_fix": "Keep the shared component but separate active from available (as v2 does) and show the itemised total inline rather than behind a link."
    }
   ]
  },
  {
   "prototype": "V1 Unified Settings",
   "findings": [
    {
     "id": "UX-V1U-1",
     "severity": "Critical",
     "confidence": 90,
     "element": "#applyChanges \u2014 \"Apply changes\" \u2192 hides the preview and shows toast \"Changes applied\"",
     "finding": "A recurring price change commits on a single click with no confirmation dialog, no proration, no card named, no amount in the button label and no undo. The only feedback is a transient toast, and the control that produced it disappears.",
     "user_impact": "An accountant who nudged a stepper to see a price can commit a billing change in one further click and have no artefact to check or reverse. Irreversible money movement behind an unlabelled primary button.",
     "suggested_fix": "Insert a review step showing now \u2192 after, proration, the card and tax, and label the button with the amount (\"Confirm and pay $X\") as v2 does."
    },
    {
     "id": "UX-V1U-2",
     "severity": "High",
     "confidence": 88,
     "element": "calcTotal() sums only transactions and seats; the three add-on cards (Invoicing $49\u2013$499, Smart rules $19.99\u2013$199.99, Historical) have their own Subscribe/Purchase buttons and feed nothing",
     "finding": "The figure presented as \"New total: $X/mo\" beside the commit button excludes every add-on on the same screen. Selecting a $199/mo Invoicing tier and clicking Subscribe changes the bill without changing the total displayed.",
     "user_impact": "The number the user commits against is wrong whenever an add-on is involved, and the error is silent \u2014 the single most damaging failure mode on a billing screen.",
     "suggested_fix": "Make one total the source of truth for every priced control on the page, add-ons included, and gate all of them behind the same commit."
    },
    {
     "id": "UX-V1U-3",
     "severity": "High",
     "confidence": 85,
     "element": "#pricePreview \u2014 display:none until a stepper moves, then rendered inline between the steppers and the Add-ons grid; not sticky",
     "finding": "The only route to commit a pending change is an inline row that appears mid-page and scrolls away with the content. Nothing else on the page indicates unsaved state \u2014 no counter, no bar, no section marker.",
     "user_impact": "On a page with eight sections, a configured-but-uncommitted change is invisible the moment the user scrolls to check the roster or the address. Silent loss of work, and no way to find the commit again except scrolling back.",
     "suggested_fix": "Pin the preview as a bottom bar for as long as the change is pending (A's savebar / v2's total bar pattern)."
    },
    {
     "id": "UX-V1U-4",
     "severity": "Medium",
     "confidence": 80,
     "element": "Per-add-on tier <select> + \"Subscribe\" / \"Purchase\" / \"Get 50 free\" buttons, each independent and unhandled",
     "finding": "Every add-on is priced and committed on its own, so the page never shows what the organization will pay after the changes; and none of the buttons have handlers, so the commit pattern for add-ons is entirely unspecified.",
     "user_impact": "A repeat user cannot answer \"what is the new monthly cost?\" from this screen at all, and each add-on becomes a separate proration event against the same term.",
     "suggested_fix": "Fold add-on tiers into the same configurator and total as quantities; one commit, one prorated charge."
    },
    {
     "id": "UX-V1U-5",
     "severity": "Medium",
     "confidence": 72,
     "element": "\u22ee menu-btn on the integration row and the user row \u2014 the only route to destructive actions on the page",
     "finding": "With just two interactive handlers on the whole page, the actions that matter (disconnect, delete a user) are hidden behind kebab menus. KF-5 records exactly this pattern as unresponsive in production and notes contextual bars beat hidden menus.",
     "user_impact": "Known dead-click zone: users click the kebab, get nothing, and re-click \u2014 the behaviour that feeds reload hammering (KF-1).",
     "suggested_fix": "Surface row actions on hover/focus as visible buttons, or as a selection-driven contextual bar."
    }
   ]
  },
  {
   "prototype": "V2 Billing Hub",
   "findings": [
    {
     "id": "UX-V2B-1",
     "severity": "Critical",
     "confidence": 88,
     "element": "#tab-billing \u2192 .tier-steps: Starter \u2192 Essential \u2192 Medium \u2192 Scale \u2192 Enterprise rendered as dots with connectors, .tier-step.current highlighted, no click handlers; adjacent \"Upgrade plan \u2192\" button calls switchTab('billing') from inside the billing tab",
     "finding": "On the Billing tab there is no working plan-change control. The tier row looks exactly like a plan picker \u2014 current state, done states, blue highlight \u2014 and is inert; the one button beside it re-selects the tab the user is already on. Quantity steppers and add-ons exist, but the plan itself cannot be changed anywhere on the screen.",
     "user_impact": "The screen's primary task is impossible, and the affordance that promises it is a five-label stepper \u2014 the densest dead-click zone in Synder production per KF-4. Users will click each tier label in turn, get nothing, and reload.",
     "suggested_fix": "Make the tier row a real selector (or replace it with plan cards) that drives the price preview, and delete the no-op \"Upgrade plan \u2192\"."
    },
    {
     "id": "UX-V2B-2",
     "severity": "High",
     "confidence": 88,
     "element": "confirmUpdate() \u2192 hides preview, shows toast \"Plan updated successfully\"",
     "finding": "The recurring-billing commit is one click to a success toast: no confirm step, no proration, no amount on the button, no card named, no undo. The function does not even carry the new values forward (txnVal = txnVal).",
     "user_impact": "Same irreversible one-click money commit as V1 Unified, with a success message that overstates what happened.",
     "suggested_fix": "Review-then-confirm with the amount in the button and the proration spelled out."
    },
    {
     "id": "UX-V2B-3",
     "severity": "High",
     "confidence": 85,
     "element": "Price preview computed from txn/usr only; add-on cards (Invoicing, Smart rules, Historical) and the tier row contribute nothing",
     "finding": "The total shown immediately above \"Update plan\" ignores every other priced control on the tab, so the number the user commits against is not the number that would be billed.",
     "user_impact": "The user reconciles against a total that is wrong whenever add-ons or a tier change are in play \u2014 and there is no second view to catch it, since the itemisation is collapsed.",
     "suggested_fix": "One total driven by every priced control on the page."
    },
    {
     "id": "UX-V2B-4",
     "severity": "Medium",
     "confidence": 78,
     "element": "\"Your plan includes\" itemisation inside a collapsed accordion (#planAccordion)",
     "finding": "How $92.00 is composed \u2014 list price, 20% yearly discount, what's included \u2014 is hidden by default on a page whose whole subject is the bill.",
     "user_impact": "The first thing an accountant checks costs a click and is easy to miss entirely; without it the headline price is unverifiable.",
     "suggested_fix": "Show the breakdown expanded by default on the Billing tab; collapse the feature list instead."
    },
    {
     "id": "UX-V2B-5",
     "severity": "Medium",
     "confidence": 72,
     "element": "Five \u22ee dot-menus across integrations and users, the only route to Refresh timezone / Disconnect / Delete",
     "finding": "Destructive and corrective actions are all behind kebabs, the pattern KF-5 flags as unresponsive in production and recommends replacing with contextual bars.",
     "user_impact": "Repeat clicking on a menu that appears not to open, on the actions with the highest consequence.",
     "suggested_fix": "Make row actions visible on hover/focus, or move them into a contextual bar on row selection."
    }
   ]
  },
  {
   "prototype": "V3 Org Health Dashboard",
   "findings": [
    {
     "id": "UX-V3-1",
     "severity": "Critical",
     "confidence": 88,
     "element": "Add-on cards with onclick=\"toggleAddon(this, 12|8|20)\" on the whole card body, calling recalcPrice() immediately \u2014 positioned BELOW the \"Update plan\" button",
     "finding": "Two failures compound. First, the entire card is a click-to-toggle billing control with no checkbox or switch: clicking a card to read its description arms a +$8\u2013$20/mo change and silently moves the total. Second, the add-ons sit beneath the commit button, so a change made after reading down the page has no commit control anywhere below it \u2014 the user must scroll back up.",
     "user_impact": "An accountant scanning three add-on cards can arm charges by clicking to read, then either commit them unknowingly along with an unrelated quantity change, or lose them by never scrolling back to the button. Money changes hands on an exploratory click.",
     "suggested_fix": "Give each card an explicit toggle with a visible state, keep the card body inert, and move the commit control below the controls it commits."
    },
    {
     "id": "UX-V3-2",
     "severity": "High",
     "confidence": 88,
     "element": "\"Update plan\" \u2192 onclick=\"showToast('Plan updated successfully!')\"",
     "finding": "Recurring billing commits on one click to a success toast \u2014 no confirm, no proration, no amount in the label, no card, no undo \u2014 and the button is placed above the add-on controls whose price it purports to commit.",
     "user_impact": "Irreversible charge with no review, plus genuine ambiguity about which of the on-screen changes it covered.",
     "suggested_fix": "Review-then-confirm naming the amount, positioned after all priced controls."
    },
    {
     "id": "UX-V3-3",
     "severity": "High",
     "confidence": 85,
     "element": "Four status cards (Subscription / Integrations / Accounting / Team) act as page navigation via activatePanel(); Organization details and Notifications sit permanently below, outside that system",
     "finding": "Two navigation models on one screen: four sections are card-gated and mutually exclusive, two are always visible below. The dashboard framing also implies health coverage for the whole org while two of six sections have no health state at all.",
     "user_impact": "A repeat user cannot tell whether \"everything\" is represented by the cards; sections outside the card set are found by accident, and the health summary reads as complete when it isn't.",
     "suggested_fix": "Either bring all six sections into the card system (with neutral states for the two settings sections) or make the cards purely a health summary and keep one consistent page structure below."
    },
    {
     "id": "UX-V3-4",
     "severity": "Medium",
     "confidence": 80,
     "element": "recalcPrice(): const isChanged = total !== basePrice + 8 \u2014 with basePrice 92, txCostPer500 4, userCostPer 6, add-ons 12/8/20",
     "finding": "Dirty state is inferred by comparing the total to a hardcoded number, so any configuration that nets to the same amount is reported as unchanged. Concretely: switch Smart Rules off (\u2212$8) and add 1,000 transactions (+$8) and the label reads \"Current plan total \u00b7 Yearly billing \u00b7 no changes\" while Smart Rules is off and the allowance is up 1,000.",
     "user_impact": "The page tells the user nothing has changed when it has, so they don't press Update plan \u2014 a swap of equal-priced items is silently discarded. Trading one add-on for another of similar price is a normal expert move.",
     "suggested_fix": "Compare the configuration object to the baseline, not the total."
    },
    {
     "id": "UX-V3-5",
     "severity": "Medium",
     "confidence": 72,
     "element": "Team roster row: \"Empty seat / \u2014 / Available\"",
     "finding": "A paid-for unused seat is rendered as a person-shaped roster row with no action on it; the only way to fill it is an \"Add User\" button elsewhere in the panel.",
     "user_impact": "Reading the roster, the count of rows no longer equals the count of people, and the row that represents an actionable asset is the one row that can't be acted on \u2014 a missing affordance where the user's attention already is.",
     "suggested_fix": "Make the empty-seat row a direct \"Invite someone to this seat\" action, or drop it and state \"1 seat available\" in the header only."
    }
   ]
  },
  {
   "prototype": "Sketch V1",
   "findings": [
    {
     "id": "UX-SK1-1",
     "severity": "High",
     "confidence": 85,
     "element": "7-row subscription table (Pro plan, Smart rules, Invoicing, RevRec, Sales volume, JS modes, Users), each row ending in \"Edit\"; no total row",
     "finding": "Seven independent per-row edit entry points and no combined total anywhere on the page. The table itemises but never sums, so the screen that looks most like an accountant's ledger never states what the organization pays.",
     "user_impact": "One intended subscription change becomes up to seven flows and seven commits, and the number the accountant came for \u2014 the total \u2014 does not exist on the page.",
     "suggested_fix": "Add subtotal / discount / total rows, and route every row's Edit into one configurator with a single commit."
    },
    {
     "id": "UX-SK1-2",
     "severity": "High",
     "confidence": 82,
     "element": "Every row of the subscription table shows Discount 20% and Price 95 USD/mo \u2014 identical across all seven rows",
     "finding": "The table's two quantitative columns carry the same value in every row, so the comparison the layout invites is impossible. A ledger whose price column is constant conveys nothing and actively misleads: it reads as if all seven line items cost the same.",
     "user_impact": "The accountant-first premise is defeated by its own content \u2014 no row can be identified as the expensive one, which is the single question this view exists to answer.",
     "suggested_fix": "Real distinct per-line amounts, right-aligned and totalled; if figures aren't available yet, mark them visibly as placeholders rather than repeating one number."
    },
    {
     "id": "UX-SK1-3",
     "severity": "Medium",
     "confidence": 78,
     "element": "\"Add more\" section listing Invoicing, RevRec, Smart rules and Historical transactions \u2014 products already present as rows in the table above",
     "finding": "The same add-ons appear twice on one screen in two different states with two different entry points: as owned line items with \"Edit\", and as upsell tiles with an add action.",
     "user_impact": "A repeat user cannot tell from the upsell block whether they already have the product; acting there risks buying something already owned.",
     "suggested_fix": "Show one entry per product with its current state, and reserve the upsell block for products not yet subscribed."
    },
    {
     "id": "UX-SK1-4",
     "severity": "Medium",
     "confidence": 75,
     "element": "All seven \"Edit\" actions \u2192 showToast('Edit <item>')",
     "finding": "Every row action is a toast; there is no drawer, dialog or destination. The row-level pattern is unspecified precisely where the money commit would live.",
     "user_impact": "The design cannot be evaluated on the thing that matters (how a paid change is reviewed and confirmed), and a user testing it learns that the primary control does nothing.",
     "suggested_fix": "Build one row's edit end to end \u2014 quantity, delta, proration, confirm \u2014 and reuse it."
    },
    {
     "id": "UX-SK1-5",
     "severity": "Medium",
     "confidence": 72,
     "element": "\"Details\" column mixing 3/10, 800/1,000, 10,000/20,000, 2/3 with \"\u2014\" on the plan row",
     "finding": "One column carries used-of-entitled ratios for six rows and nothing for the seventh, with no header explanation of which number is consumption and which is allowance.",
     "user_impact": "An expert scanning for \"what am I about to outgrow?\" has to decode the column's meaning per row instead of reading it, and the plan row \u2014 the one that sets the allowances \u2014 is blank.",
     "suggested_fix": "Split into explicit Used and Included columns with a usage bar, and populate the plan row."
    }
   ]
  },
  {
   "prototype": "Sketch V2 Tabbed",
   "findings": [
    {
     "id": "UX-SK2-1",
     "severity": "High",
     "confidence": 85,
     "element": "Users tab: \"Users available for this organization: 2.\" above a roster of three (Lara Croft, Russel Crow, Monica Belucci); no seat control on any of the five tabs",
     "finding": "The seat allowance is stated but not actionable \u2014 there is no \"Additional users\" row in the Billing tab's add-on list and no seat control in the Users tab. The roster also shows three entries against two seats, so the stated limit and the visible reality disagree.",
     "user_impact": "Resizing the team, one of the most common billing tasks, cannot be completed anywhere on the screen, and the number that would tell the user they need to is contradicted by the list beneath it.",
     "suggested_fix": "Put a seat quantity control in the Users tab next to the count, committing through the same flow as other paid changes, and reconcile the count with the roster."
    },
    {
     "id": "UX-SK2-2",
     "severity": "High",
     "confidence": 85,
     "element": "Add-on rows (Monthly transactions, Invoicing, Smart rules, Revenue recognition, Historical transactions) each showing \"$0.00 USD\" with an add action \u2192 showToast('Invoicing added')",
     "finding": "Each row commits a paid subscription on one click, with $0.00 shown as the price, no total on the page, no confirm and no undo. The success toast asserts the add-on is live.",
     "user_impact": "A user adds paid products believing they are free, with nothing on screen to correct the impression and no way to reverse it. This is the worst commit-moment on any of the eleven: maximum consequence, zero information, zero friction.",
     "suggested_fix": "Show each add-on's real price and tier, sum them into one page total, and commit everything through a single review step."
    },
    {
     "id": "UX-SK2-3",
     "severity": "High",
     "confidence": 85,
     "element": "All five add-on rows carry the identical description: \"Benefit from flat fee rather than commission on every invoice amount!\"",
     "finding": "The description column is the same sentence five times, and it describes Invoicing specifically while sitting on Monthly transactions, Smart rules, RevRec and Historical transactions.",
     "user_impact": "The list cannot be used to tell products apart, so the purchase decision is made on the name alone \u2014 next to a one-click commit and a $0.00 price. Users buy the wrong add-on.",
     "suggested_fix": "Distinct one-line descriptions per add-on, with the tier and price on the same row."
    },
    {
     "id": "UX-SK2-4",
     "severity": "Medium",
     "confidence": 78,
     "element": "Five sidebar tabs (connections / billing / users / orgname / notifications) with no cross-links",
     "finding": "Plan and add-ons live on Billing while the roster lives on Users, and neither references the other. Team size is a billing quantity managed on a tab that shows no prices.",
     "user_impact": "Any team-size change spans two tabs with a full context switch, and the user must remember the seat count across the jump.",
     "suggested_fix": "Cross-link the two, and show the seat cost inline in the roster header."
    },
    {
     "id": "UX-SK2-5",
     "severity": "Medium",
     "confidence": 72,
     "element": "\"\u00b7\u00b7\u00b7\" menus as the only route to Edit / Remove / Resend invite on both the integrations table and the users table",
     "finding": "Row actions are hidden behind ellipsis menus throughout, the pattern KF-5 records as unresponsive in production.",
     "user_impact": "Known dead-click zone on the actions with the highest consequence (remove a user, disconnect an integration), producing repeat clicks.",
     "suggested_fix": "Visible row actions on hover/focus, or a contextual bar on selection."
    }
   ]
  },
  {
   "prototype": "Sketch V2 Long page",
   "findings": [
    {
     "id": "UX-SK3-1",
     "severity": "High",
     "confidence": 85,
     "element": "Add-on rows in the \"Addons\" section \u2014 \"$0.00 USD\" each, identical description copy, one-click add \u2192 showToast('Monthly transactions added')",
     "finding": "Same instant-commit add-on pattern as the tabbed version \u2014 $0.00 prices, five identical descriptions, no total, no confirm, no undo \u2014 now embedded in one continuous scroll, so the commit sits even further from any billing context.",
     "user_impact": "Users add paid products on one click believing they are free; on a long page there is no persistent total or bar anywhere to contradict it.",
     "suggested_fix": "Real prices and distinct descriptions per row, one page-level total, one review step before any charge."
    },
    {
     "id": "UX-SK3-2",
     "severity": "High",
     "confidence": 82,
     "element": "Users section: \"Users available for this organization: 2.\" above three roster rows; no seat control anywhere on the page",
     "finding": "The seat allowance is stated, contradicted by the roster (three people), and not adjustable \u2014 no seat row exists in the Addons section and no control exists in the Users section.",
     "user_impact": "Team resizing is impossible from the page that presents the seat count, and the count itself can't be trusted against the list beside it.",
     "suggested_fix": "Add a seat quantity control beside the count, priced and committed like other paid changes."
    },
    {
     "id": "UX-SK3-3",
     "severity": "Medium",
     "confidence": 78,
     "element": "Section order: Connections \u2192 Plan and billing \u2192 Addons \u2192 Users \u2192 Organization name \u2192 Notifications",
     "finding": "A page reached to manage a subscription opens on integration status; \"Plan and billing\" is the second section and \"Addons\" the third, both below the fold.",
     "user_impact": "The highest-frequency destination is never at the top, so every repeat visit begins with a scroll past connection status the user did not come for.",
     "suggested_fix": "Lead with plan, billing and add-ons; move connection health to a compact banner at the top if it needs attention."
    },
    {
     "id": "UX-SK3-4",
     "severity": "Medium",
     "confidence": 80,
     "element": "Form fields labelled \"Setting name ?\" throughout Organization name and Notifications, with the real label rendered as adjacent text",
     "finding": "Every field on the page carries the placeholder label \"Setting name\" plus a \"?\" tooltip, while the actual purpose (Organization name, Time zone, Notify about the app activity, Reconciliation reminder) sits beside it as separate text.",
     "user_impact": "No field states what it does in its own label, so on a six-section page the user pairs labels to controls by proximity \u2014 and the \"?\" affordance repeated on every row trains them to ignore it.",
     "suggested_fix": "Put the real label on the field and reserve the tooltip for genuine explanation."
    },
    {
     "id": "UX-SK3-5",
     "severity": "Medium",
     "confidence": 78,
     "element": "No sticky total, save bar or dirty indicator anywhere on the long page; every commit is a local instant toast",
     "finding": "Across six sections and multiple paid controls there is no single place that states what the organization pays or what is pending. Notification changes toast \"Saved\", add-ons toast \"added\", and nothing aggregates.",
     "user_impact": "After a session of edits the user has no way to answer \"what will I be billed?\" \u2014 and no way to verify which of the toasts represented a real charge.",
     "suggested_fix": "Pin one bar carrying the current total and any pending change, and route all paid changes through it."
    }
   ]
  }
 ],
 "ranking": [
  {
   "rank": 1,
   "prototype": "v2",
   "why": "The only prototype where the money commit is unmistakable and the consequences are named: the confirm button carries the amount and, on a mid-term downgrade, the exact forfeit (\"Downgrade now anyway (lose $X)\"), plus a non-destructive alternative (\"Schedule it for renewal instead\"). Blocked downgrades name who loses access and offer one-click fixes, and the fixed total bar always shows the delta. Its faults are late-surfacing blocks and one genuine dead-end (Smart rules), not the commit model."
  },
  {
   "rank": 2,
   "prototype": "A",
   "why": "Fastest expert path: one page with anchors, one drawer holding all four quantities with a live footer total and a review step. Loses to v2 because four different commit contracts coexist on one screen and the historical-transactions purchase charges on a single click while a reversible seat change is gated \u2014 the commit weight is inverted for the one irreversible action."
  },
  {
   "rank": 3,
   "prototype": "C",
   "why": "Best-articulated commit model of the eleven \u2014 \"nothing has been charged or saved yet\", one combined confirm, a real now \u2192 after diff. Ranked below A on density and repeat cost: nine read-only cards mean every edit is card \u2192 panel \u2192 wizard \u2192 cart \u2192 confirm, the pending state survives only as a header badge after a 4.4s toast (KF-2), and free edits are coupled to paid ones."
  },
  {
   "rank": 4,
   "prototype": "B",
   "why": "Honest itemised sticky total and steppers adjacent to what they price. But the matrix cannot do the one thing a matrix is for \u2014 steppers are live only in the selected column and Premium never shows a price \u2014 and 28 rows with ~300px of permanent chrome means every repeat visit re-scrolls a marketing comparison table to change one quantity."
  },
  {
   "rank": 5,
   "prototype": "Concepts 3-in-1",
   "why": "Strongest information design of round 1 \u2014 V1's itemised ledger is the closest any prototype gets to what an accountant actually reads. But nothing is interactive, so the commit path is entirely unspecified; V1 fragments one change into six row-level flows with no combined total; and the headline figure is labelled both \"next invoice\" and \"monthly total\" on a yearly cycle."
  },
  {
   "rank": 6,
   "prototype": "V3 Org Health Dashboard",
   "why": "The health-card triage is the best at-a-glance layer in round 1 and the price preview updates live. Sunk by the money surface: whole-card click-to-toggle add-ons that arm charges on an exploratory click, placed below the commit button, a one-click \"Update plan\" with no review, and a dirty check comparing the total to a hardcoded constant so an equal-priced swap reports \"no changes\"."
  },
  {
   "rank": 7,
   "prototype": "V1 Unified Settings",
   "why": "Coherent single-page structure with steppers and a live preview, but the preview excludes every add-on on the same screen, \"Apply changes\" commits recurring billing in one click to a toast with no amount or undo, and that commit control is a non-sticky inline row that scrolls out of view."
  },
  {
   "rank": 8,
   "prototype": "Sketch V1",
   "why": "The right instinct \u2014 one itemised table an accountant can read \u2014 executed with a price column that shows the same 95 USD/mo in all seven rows, no total, seven separate Edit flows, and add-ons duplicated between the owned table and the upsell block."
  },
  {
   "rank": 9,
   "prototype": "V2 Billing Hub",
   "why": "Placed here despite reasonable structure because the primary task is impossible: the five-tier stepper that looks exactly like the plan picker has no handler, and the \"Upgrade plan \u2192\" beside it re-selects the current tab. A dead five-label stepper is the worst possible location for a dead click given KF-4, and the commit that does work fires with no review."
  },
  {
   "rank": 10,
   "prototype": "Sketch V2 Long page",
   "why": "One-click paid add-ons priced at $0.00 with five identical descriptions, no total or save bar anywhere on six sections, a stated seat count that is both unactionable and contradicted by the roster, and every field labelled \"Setting name\". Its working scroll-spy nav is the one thing it does better than the tabbed version."
  },
  {
   "rank": 11,
   "prototype": "Sketch V2 Tabbed",
   "why": "Same broken add-on commit, same $0.00 prices, same identical copy and same unactionable seat count as the long page, with the content split across five tabs that add a navigation cost and separate team size from the billing controls that should govern it. Ranked last because tabbing makes the same defects more expensive to reach."
  }
 ],
 "verdict": "Ten of the eleven prototypes let a recurring or one-time charge commit on a single click to a success toast \u2014 no amount in the button, no proration, no card named, no undo \u2014 and four of them show a total beside that button that provably excludes other priced controls on the same screen. v2 is the only one that treats the commit moment as the design problem: it names the amount, names the forfeit, blocks downgrades that would break the org with specific consequences, and offers a reversible alternative. For the expert path the decisive variable is where the total lives: A and v2 keep one live figure next to the controls and win on repeat cost, while C's per-field click tax, B's 28-row matrix and the sketches' total-free instant commits all make the monthly job slower or unverifiable."
}